# CLAUDE.md — AI Sana Challenge Hub

**Прочитай `AGENTS.md`, затем `backend/AGENTS.md`** перед первой задачей: общий контекст, правила
хакатона, зоны, кто из агентов какими файлами владеет. Фронт (`web/`) — зона напарника, не трогать.

## Правила работы

1. Отвечать по-русски.
2. Контракт API зафиксирован: нужно новое поле — сначала правка `docs/02-api.md` (и копии
   `partner/01-api.md`), потом код.
3. Комментарии в коде — минимум, только в неочевидных местах, по-русски.
4. Не расширять скоуп сверх ТЗ: всё лишнее — после демо.
5. Код замораживается в 17:40. Коммит и push — только когда Арслан просит, только своя зона,
   перед push — `git pull --rebase`.
